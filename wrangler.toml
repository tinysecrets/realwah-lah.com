# Cloudflare Pages — optional CLI deploy (dashboard Git connect is preferred)
name = "wah-lah"
compatibility_date = "2024-01-01"
pages_build_output_dir = "frontend/build"

[vars]
# Set REACT_APP_BACKEND_URL in Cloudflare Pages dashboard for production builds.
